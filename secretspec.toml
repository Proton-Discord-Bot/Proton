[project]
name = "Proton"
revision = "1.0"


[profiles.default]
TOKEN = { description = "Discord bot token", required = true }
DISCORD_APPLICATION_ID = { description = "Discord application id, used to register commands", required = true }
DATABASE_PATH = { description = "SQLite database file path", required = false }

[profiles.development]
# Inherits everything from default, and adds one secret that only makes sense here.
DISCORD_GUILD_ID = { description = "Dev guild to register commands to for instant updates", required = false }

[profiles.production]
# Inherits TOKEN and DISCORD_APPLICATION_ID as required from the default profile.
DATABASE_PATH = { description = "Absolute path to the SQLite database file", required = true }
