[project]
name = "Proton"
revision = "1.0"

# Extend configurations from subdirectories
# extends = [ "subdir1", "subdir2" ]

[profiles.default]
TOKEN = { description = "Discord bot token", required = true }
DISCORD_APPLICATION_ID = { description = "Discord application id, used to register commands", required = true }
# Optional: path to the SQLite file; defaults to ./database.sqlite
DATABASE_PATH = { description = "SQLite database file path", required = false }

[profiles.development]
# Inherits everything from default, and adds one secret that only makes sense here.
#
# Guild-scoped command registration updates instantly, whereas global registration can take
# up to an hour to propagate. `bun run deploy` registers to this guild when the variable is
# present and globally when it is absent, so it is declared only in this profile — that way
# a production deploy cannot accidentally confine every command to a single test server.
DISCORD_GUILD_ID = { description = "Dev guild to register commands to for instant updates", required = false }

[profiles.production]
# Inherits TOKEN and DISCORD_APPLICATION_ID as required from the default profile.
#
# DATABASE_PATH is required here, unlike in default. Its fallback (./database.sqlite) is
# resolved against the working directory, so a service unit started from a different cwd
# would silently create a second, empty database instead of opening the real one. Pin it
# to an absolute path.
DATABASE_PATH = { description = "Absolute path to the SQLite database file", required = true }
# DISCORD_GUILD_ID is intentionally absent here — it is declared only in the development
# profile, so commands always register globally in production.
